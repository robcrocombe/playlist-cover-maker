import type { SimplifiedPlaylist } from '@spotify/web-api-ts-sdk';

interface PlaylistTabProps {
  playlist: SimplifiedPlaylist;
}

export function PlaylistTab({}: PlaylistTabProps): JSX.Element {
  return <div></div>;
}
